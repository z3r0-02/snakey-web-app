import en from "./locales/en";
import cs from "./locales/cs";

// Translations live in one file per locale under ./locales.
// To add a language: create ./locales/<code>.js and add it here.
export const dictionaries = { en, cs };
